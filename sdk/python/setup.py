from setuptools import setup, find_packages

setup(
    name="vectordb-client",
    version="1.0.0",
    description="High-Throughput Distributed Vector Search Engine Python SDK",
    packages=find_packages(),
    python_requires=">=3.8",
)
